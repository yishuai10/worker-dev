spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      config:
        server-addr: 192.168.0.250:8848
        username: nacos
        password: nacos
        name: gateway-service.yml
        namespace: worker-dev_dev
        file-extension: yaml
        group: dev
