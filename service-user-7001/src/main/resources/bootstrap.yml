spring:
  application:
    name: user-service
  cloud:
    nacos:
      config:
        server-addr: 192.168.0.250:8848
        username: nacos
        password: nacos
        name: user-service.yml
        namespace: worker-dev_dev
        file-extension: yaml
        group: dev
